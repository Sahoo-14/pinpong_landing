# OS files
.DS_Store
Thumbs.db

# Editor folders
.vscode/
.idea/

# Node (in case a build tool / dev server gets added later)
node_modules/
package-lock.json
npm-debug.log*

# Env files (in case waitlist backend keys get added later)
.env
.env.local

# Build output (in case a bundler gets added later)
dist/
build/
